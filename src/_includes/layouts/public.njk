{# Bước 1: Kế thừa từ layout base #} {% extends "layouts/base.njk" %} {# Bước 2:
Điền nội dung vào block 'content' của base.njk #} {% block content %}
<div class="min-h-screen">
  <div class="flex-1 flex flex-col justify-between min-h-screen">
    {% include "partials/header.njk" %}
    <main>
      {% block mainContent %}{% endblock %}
      {% include "components/FloatTopAction.njk" %}
    </main>
    {% include "components/footer.njk" %}
  </div>
</div>
{% endblock %} {% block scripts %}
{{ super() }}

{# Tải các script chung cho TOÀN BỘ khu vực admin #}

<script src="{{ site.baseLink }}/js/lib.js"></script>
<script src="{{ site.baseLink }}/js/aos.js"> </script>
<script>
  window.addEventListener('load', function () {
    // AOS.init({
    //   once: true
    // });
    AOS.init();
    $('body').addClass('remove-after');
    setTimeout(() => {
      $('body').removeClass('loading-page remove-after');
    }, 600);
  });
</script>
<script src="{{ site.baseLink }}/js/fontawesome.js"></script>
<script src="{{ site.baseLink }}/js/app.js"></script>
{# TẠO MỘT "KHE CẮM" MỚI: Đây là nơi dành riêng cho các trang con như
dashboard.njk để chèn script của riêng chúng vào. #} {% block pageScripts %}{%
endblock %} {% endblock %}
